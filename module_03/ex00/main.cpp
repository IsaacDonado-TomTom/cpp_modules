#include "ClapTrap.hpp"

int main(void)
{
    ClapTrap first("Brandon");
    first.attack("Sussy");
    first.takeDamage(4);
    first.beRepaired(10);
    first.takeDamage(20);
    first.attack("Robbie");
    return (0);
}